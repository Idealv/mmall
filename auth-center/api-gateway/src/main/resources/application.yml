zuul:
  routes:
    auth-server:
      path: /oauth/**
      url: http://localhost:9999/
      sensitive-headers:
server:
  port: 8888
spring:
  application:
    name: zuul
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
